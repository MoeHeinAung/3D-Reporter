"""
Service layer — all business logic and domain rules live here.

Services depend on repositories (constructor injection). They never access
the database directly. Each service encapsulates a specific domain concern.
"""
